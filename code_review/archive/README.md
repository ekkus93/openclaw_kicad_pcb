# code_review archive

Historical code-review artifacts, generated schematics, and evaluation outputs
are stored in dated subdirectories:

- `2026-06-09/` — original `code_review/` contents archived during the
  KICAD_WEB_REFACTOR_FIX2 cleanup

Active model-corpus evaluation output (generated by `kicad_pcb.cli model-corpus evaluate`)
goes to `data/model_eval/` at the project root, not here.
